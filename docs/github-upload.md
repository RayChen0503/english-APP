# GitHub 上傳說明

目前本機環境沒有 `git` 或 `gh` 指令，因此我無法直接用命令列建立 commit 或 push。

可行路徑：

1. 你提供一個已存在的 GitHub repository，例如 `owner/repo`。
2. 我可以使用 GitHub connector 將文字檔逐一建立到該 repository。
3. 若要完整上傳 Android 專案，建議先讓本機安裝 Git，或提供一個空 repo 後，我再用 connector 上傳主要原始碼。

建議 repo 名稱：

```text
sora-companion-android
```
